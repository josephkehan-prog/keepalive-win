interface:
  display_name: "Keepalive Win"
  short_description: "Repo-specific patterns and workflows for keepalive-win"
  default_prompt: "Use the keepalive-win repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true